<div class="container pt-4">
  <div class="card">
    <div class="card-body">
      <h1 class="card-title"><%= @event.name %></h1>
      <p class="card-text"><strong>Date:</strong> <%= @event.date.strftime('%B %d, %Y') %></p>
      <%= link_to "Back", :back, style: "text-decoration: none;",  class: "text-end"%>
    </div>
  </div>
</div>
